% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sc-markers.R
\name{sc_markers_summary}
\alias{sc_markers_summary}
\title{Filter and rank markers; optionally export a per-cluster summary}
\usage{
sc_markers_summary(
  obj,
  top_n = 30,
  log2fc_cut = 1,
  padj_cut = 0.05,
  output_path = "markers_top_per_cluster.txt",
  rationale = NULL
)
}
\arguments{
\item{obj}{An AgentSeurat object after [sc_find_markers()].}

\item{top_n}{Integer, top markers per cluster. Default 30.}

\item{log2fc_cut}{Numeric. Default 1.}

\item{padj_cut}{Numeric. Default 0.05.}

\item{output_path}{Optional path to write the text summary. If NULL,
no file is written. Default "markers_top_per_cluster.txt".}

\item{rationale}{Optional LLM-supplied rationale.}
}
\value{
Updated AgentSeurat; filtered table is stored at
  `obj@params$markers_filtered` and the per-cluster summary list at
  `obj@params$markers_summary`.
}
\description{
Applies the ranking used in the Ca_Ctrl pipeline: keep markers with
`avg_log2FC > log2fc_cut` and `p_val_adj < padj_cut`, then rank within
each cluster by `pct.1 - pct.2` (specificity) and keep the top N.
Writes a compact text summary in the form
`cluster0:Gene1,Gene2,...` (convenient for pasting into ACT/CellMarker).
}
