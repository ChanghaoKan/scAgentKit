% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/qc-add-metrics.R
\name{qc_add_metrics}
\alias{qc_add_metrics}
\title{Add standard QC metrics to the underlying Seurat data}
\usage{
qc_add_metrics(
  obj,
  species = c("auto", "mouse", "human"),
  mt_pattern = NULL,
  ribo_pattern = NULL,
  hb_pattern = NULL,
  rationale = NULL
)
}
\arguments{
\item{obj}{An AgentSeurat object.}

\item{species}{Character, one of `"auto"`, `"mouse"`, or `"human"`.
Defaults to `"auto"`, which infers mouse versus human from gene-name
conventions and asks for an explicit choice when detection is ambiguous.
Ignored if all three custom patterns are supplied.}

\item{mt_pattern}{Optional custom regex for mitochondrial genes.}

\item{ribo_pattern}{Optional custom regex for ribosomal genes.}

\item{hb_pattern}{Optional custom regex for hemoglobin genes.}

\item{rationale}{Optional LLM-supplied explanation of why this species
choice was made. Defaults to a templated string.}
}
\value{
Updated AgentSeurat object with three new metadata columns on
  the underlying Seurat data and a new entry in the decision log.
}
\description{
Computes `percent.mt`, `percent.ribo`, and `percent.hb` based on gene-name
regex patterns. Applies to either a single Seurat object or every element
of a Seurat list, depending on the current `data_type`. Species-specific
defaults are provided; pass `mt_pattern`, `ribo_pattern`, or `hb_pattern`
to override.
}
\examples{
\dontrun{
  obj <- AgentSeurat(seurat)
  obj <- qc_add_metrics(obj, species = "human")
}
}
