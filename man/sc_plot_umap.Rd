% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sc-plot-umap.R
\name{sc_plot_umap}
\alias{sc_plot_umap}
\title{Save UMAP plots and register them in @figures}
\usage{
sc_plot_umap(
  obj,
  group_bys = "seurat_clusters",
  split_by = NULL,
  out_dir = "figures",
  tag = NULL,
  width = 7,
  height = 5,
  dpi = 150,
  rationale = NULL
)
}
\arguments{
\item{obj}{An AgentSeurat object after [sc_umap()].}

\item{group_bys}{Character vector of metadata columns to plot. If any
of "cluster" / "clusters" / "seurat_clusters" is requested it is
mapped to `seurat_clusters`.}

\item{split_by}{Optional metadata column for facetted plots.}

\item{out_dir}{Directory for PNG output. Default "figures".}

\item{tag}{Optional filename suffix.}

\item{width, height, dpi}{Plot dimensions / resolution.}

\item{rationale}{Optional LLM-supplied rationale.}
}
\value{
Updated AgentSeurat.
}
\description{
Convenience wrapper that draws UMAPs colored by one or more metadata
columns (and optionally by `seurat_clusters`), saves them to disk, and
registers the paths. Having plots on disk lets a vision-capable LLM
inspect the embedding when deciding next steps (e.g. whether integration
succeeded, whether clusters make sense).
}
