% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/qc-doublet.R
\name{qc_doublet}
\alias{qc_doublet}
\title{Per-sample doublet detection with scDblFinder}
\usage{
qc_doublet(obj, remove = TRUE, seed = 999, rationale = NULL)
}
\arguments{
\item{obj}{An AgentSeurat object with `data_type == "seurat_list"`.}

\item{remove}{Logical. If TRUE (default), cells classified as doublets
are removed in place. If FALSE, doublet metadata is added but cells
are kept, allowing downstream inspection.}

\item{seed}{Integer seed for reproducibility. Default 999.}

\item{rationale}{Optional LLM-supplied rationale.}
}
\value{
Updated AgentSeurat object with `doublet_class` and
  `doublet_score` in the metadata of each sample.
}
\description{
Runs scDblFinder on each element of a Seurat list and optionally removes
detected doublets. Running *per sample* (rather than on a merged object)
is critical: merged-object doublet detection inflates false positives
because inter-sample cell-type mixing is misread as heterotypic doublets.
}
\details{
Requires `data_type == "seurat_list"`. Call [qc_split()] first.
}
