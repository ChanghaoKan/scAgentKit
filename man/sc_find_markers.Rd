% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sc-markers.R
\name{sc_find_markers}
\alias{sc_find_markers}
\title{Find marker genes for all clusters}
\usage{
sc_find_markers(
  obj,
  only_pos = FALSE,
  min_pct = 0.25,
  logfc_threshold = 0,
  rationale = NULL
)
}
\arguments{
\item{obj}{An AgentSeurat object after [sc_cluster()].}

\item{only_pos}{Logical. Default FALSE (keep down-regulated markers too,
which can be informative for negative markers in annotation).}

\item{min_pct}{Numeric. Default 0.25.}

\item{logfc_threshold}{Numeric. Default 0 (no pre-filter; filter later).}

\item{rationale}{Optional LLM-supplied rationale.}
}
\value{
Updated AgentSeurat; marker table is stored at
  `obj@params$all_markers`.
}
\description{
Wrapper around [Seurat::FindAllMarkers()]. Returns the full marker table
and stores it at `obj@params$all_markers` for downstream filtering or
annotation. Use [sc_markers_summary()] to get the
filtered + pct_diff-ranked top-N-per-cluster summary.
}
